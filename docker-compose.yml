version: "3"
services:

  app:
    image: ortussolutions/commandbox:lucee5-snapshot
    volumes:
      - ./app/:/app/
    ports:
      - "80:8080"
    environment:
      - CFENGINE=lucee@5
      - cfconfig_adminPassword=${ADMIN_PASSWORD:-commandbox}
